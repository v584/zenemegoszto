import { ArtistsListPipe } from './artists-list.pipe';

describe('ArtistsListPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistsListPipe();
    expect(pipe).toBeTruthy();
  });
});
